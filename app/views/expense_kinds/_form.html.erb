<%= form_with(model: expense_kind, local: true) do |form| %>
  <% if expense_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense_kind.errors.count, "error") %> prohibited this expense_kind from being saved:</h2>

      <ul>
        <% expense_kind.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Tipo" %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.button '<i class="material-icons right">save</i>Salvar'.html_safe, class: "blue-grey btn waves-effect waves-light" %>
  </div>
<% end %>
