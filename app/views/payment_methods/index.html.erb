<p id="notice"><%= notice %></p>

<h1>Formas de Pagamento</h1>
<br>
<%= link_to '<i class="material-icons left">receipt</i>Novo Método de Pagamento'.html_safe, 
            new_payment_method_path, 
            class: "blue-grey btn waves-effect waves-light" %>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payment_methods.each do |payment_method| %>
      <tr>
        <td><%= payment_method.name %></td>
        <td><%= link_to 'Mostrar', payment_method %></td>
        <td><%= link_to 'Editar', edit_payment_method_path(payment_method) %></td>
        <td><%= link_to 'Apagar', payment_method, method: :delete, data: { confirm: 'Tem certeza de que deseja apagar o registro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>